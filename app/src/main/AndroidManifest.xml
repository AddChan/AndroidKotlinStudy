<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="add.chan.sichan.androidkotlinstudy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Tutorial1.ApplicationActivity" />
        <activity
            android:name=".Tutorial2_BmiCalculator.BmiResultActivity"
            android:parentActivityName=".Tutorial2_BmiCalculator.BmiCalculatorActivity" />
        <activity android:name=".Tutorial2_BmiCalculator.BmiCalculatorActivity" />
        <activity android:name=".Tutorial3_StopWatch.StopWatchActivity"></activity>
    </application>

</manifest>